[tool:pytest]
# pytest配置文件
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# 排除不是测试文件的文件
collect_ignore = app.py test_app.py venv instance repos logs __pycache__

# 禁用输出捕获以避免I/O错误
addopts = -s --tb=short

# 设置最小pytest版本
minversion = 6.0

---
description: AI coding guidance and rules for this repository.
alwaysApply: true
---
<rule>

# 配表代码版本 Diff 平台 - AI 编码规则（与当前代码对齐）

## 1. 架构事实（必须对齐）

### 1.1 启动与生命周期拆分
- `app.py`：应用对象装配、蓝图注册、依赖注入、兼容入口函数暴露。
- `bootstrap/app_factory.py`：创建 Flask App，规范化运行时配置（`RuntimeSettings`）。
- `bootstrap/bootstrap.py`：`AppBootstrapManager` 负责 `initialize_app/cleanup_app` 生命周期。
- `bootstrap/runtime_entry.py`：`__main__` 实际入口，负责信号处理、模式分流、启动 Flask 或 Agent。

### 1.2 部署模式
- `single`：启动 Flask + 本地后台 worker + scheduler（含清理任务）。
- `platform`：启动 Flask + scheduler（仅任务分发，不启本地 worker）。
- `agent`：不启动 Flask，直接运行 `agent.runner_runtime.run_agent()`。

### 1.3 分层约定
- `routes/*`：路由绑定（Blueprint）。
- `services/*_handlers.py`：请求参数、权限、返回格式、业务编排。
- `services/*_service.py` / `services/*_logic.py`：核心业务逻辑。
- `models/*`：ORM 与数据结构。

新功能默认落到 `routes/` + `services/`，避免继续向 `app.py` 堆积职责。

## 2. 依赖与模块组织

### 2.1 禁止循环导入的约定
- `services/` 内不要在模块顶层 `from app import app`。
- 需要运行时对象时，优先走 `services/model_loader.py`：
  - `get_runtime_model(name)`
  - `get_runtime_models(*names)`

### 2.2 依赖注入约定
- 需要 `app/db/服务实例` 的模块，使用 `configure_*()` 注入，不在导入阶段绑定。
- 新服务模块若涉及运行时状态，必须提供清晰的 `configure_*()` 入口。

## 3. SQLAlchemy 2.x 规范

### 3.1 主键读取
- 禁止新增 `Query.get()`（legacy）。
- 优先使用 `db.session.get(Model, id)`。
- 路由层需要自动 404 时可继续使用 `Model.query.get_or_404(id)`。

### 3.2 事务处理
- 写操作必须显式 `commit`。
- 异常路径必须 `rollback` 并记录日志。
- 避免循环内单条查询引发 N+1，必要时批量查询或预加载。

## 4. 任务调度与并发

### 4.1 scheduler（`services/task_worker_service.py`）
- 每天 `04:00`：缓存清理（仅 `include_cleanup=True` 时注册）。
- 每 `2` 分钟：周版本同步任务扫描。
- 每 `2` 分钟：仓库自动同步任务扫描。

### 4.2 worker 与模式边界
- `single` 模式运行本地后台任务处理。
- `platform/agent` 模式下，本地 worker 关闭，任务走 Agent 分发链路。
- 修改调度周期、任务类型或任务状态机时，必须同步更新测试与文档。

## 5. 安全与权限

### 5.1 请求安全
- 所有修改类请求（POST/PUT/DELETE）必须通过 CSRF 校验。
- 权限控制优先用既有装饰器与安全工具，不在路由中散落硬编码判断。

### 5.2 凭据与路径
- 仓库密钥/密码使用 `utils/security_utils.py` 的加解密能力。
- 仓库本地路径必须通过 `utils/path_security.py` 构建与校验。

### 5.3 资源访问错误反馈
- 资源不存在/项目已删除/仓库已删除/周版本已删除 与 权限不足 要给出可区分提示。
- 使用统一错误页面模板并提供返回主页入口，避免直接抛裸异常。

## 6. 日志、返回与前端交互

### 6.1 日志
- 业务日志优先 `log_print(...)`，保留统一日志分类。
- 启动链路的诊断输出可使用 `_original_print`（仅限启动相关代码）。

### 6.2 API 返回
- JSON 返回结构保持一致：
  - 成功：`success/status` + `message` + 可选 `data`
  - 失败：明确状态码（400/401/403/404/500）与可读错误信息

### 6.3 前端行为约定
- 列表批量操作、右键菜单、快捷键（如 shift 连选）等交互需保证状态一致性。
- 多选逻辑、上下文菜单逻辑变更时必须补充前端行为测试或最小回归脚本。

## 7. 测试与变更要求

### 7.1 测试基线
- 新增或修改行为时，至少补一条对应 pytest 用例（正常路径 + 关键边界）。
- 与部署模式、Agent 分发、自更新相关改动，优先补 `tests/test_agent_*`。
- SQLAlchemy 访问方式改造需要包含“无 legacy API”回归验证。

### 7.2 文档同步
- 涉及启动方式、部署模式、发布/回滚、自更新配置变化时：
  - 更新 `README.md`
  - 更新 `平台配置说明.md`
  - 必要时更新 `代码架构说明.md`

## 8. 禁止事项

1. 禁止新增 `Query.get()` 调用。
2. 禁止在 `services/` 顶层直接依赖 `app` 全局对象。
3. 禁止将敏感信息（密码、Token、私钥）硬编码进仓库。
4. 禁止绕过权限校验直接暴露管理接口。
5. 禁止在错误路径缺少回滚导致脏事务残留。
6. 禁止修改任务分发链路后不补测试。
7. 禁止在app.py等入口文件编写完整逻辑，改成使用其他文件实现，app.py调用方法。

</rule>

/repos
/venv
venv/
.venv/
env/

/.codemaker/rules/rules.mdc
.codex_runtime

openspec/

/instance/
*.db
*.sqlite
*.sqlite3

/logs/
*.log

__pycache__/
*/__pycache__/
test.py
*.pyc
*.pyo
*.pyd
*.swp
.pytest_cache/
.pytest_tmp/
tests/_pytest_tmp_run/

# 工具（Claude Code）的工作树目录：并发 agent 在这里建临时 git worktree，
# 属于本机工具状态、不是源码。不忽略的话 `git status` 会一直挂着一堆未跟踪目录，
# 而且很容易被 `git add -A` 顺手带进提交。
.claude/worktrees/
.mypy_cache/
.ruff_cache/

# 编码助手（Claude Code 等）落在工作目录里的会话流水，形如
# `.appdump/sessions/<session-id>.events.ndjson`。它是**本地工具产物**，不是源码：
# 内容包含完整的对话记录与工具输出（可能夹带线上数据、路径、被探测过的配置值），
# 且每次会话都换一份。绝不入库。
/.appdump/

.coverage
coverage.xml
htmlcov/

.env
.env.*
!.env.simple
!agent/.env.simple

.vscode/
.idea/
node_modules/

agent/agent_package*
agent/venv

_startup_log.txt  
_stdout_restart.txt

findings.md
progress.md
task_plan.md  

test_output.txt

docs/notes/

# 项目专属 skill 目录由平台运行期创建/上传（见 skills/projects/）。
# 它们属于部署实例的数据，不是源码，因此默认忽略；随仓库分发的那份用 `!` 放行。
# 注意：git 不会进入被排除的目录，所以必须先放行目录本身，其内容才可能被跟踪。
skills/projects/*/
!skills/projects/g119/

# ---------------------------------------------------------------------------
# AI 分析这一轮的**一次性清理脚本**与**审计/方案类文档**：用户明确不提交。
#
# 一个个列名而不是写 `docs/AI分析*.md`：通配会连将来新加的、你真想提交的文档一起吞掉，
# 而被吞掉的症状是「git status 里看不见它」—— 安静得很。要放行某一条，删掉对应那行即可。
#
# **这些不算临时品，是交付物，没有忽略**（列在这里只是为了避免被上面那个想法误伤）：
#   scripts/export_ai_benchmark_sample.py + benchmarks/ai_analysis/**（任务 H 的工具链）
#   scripts/clean_ruling_block_from_runs.py —— ★ 它**有已提交的测试依赖**：
#     tests/test_ai_route_wiring_and_claim_migration.py 里有 3 条要 exec 它。
#     曾经被错误地忽略在这里，结果是**本地绿、CI 红**（FileNotFoundError:
#     .../scripts/clean_ruling_block_from_runs.py）。判据很简单：
#     **凡是被 tests/ 引用到的文件都不是「临时品」，不许进这个名单。**
# ---------------------------------------------------------------------------
scripts/clean_probe_residue.py
docs/AI分析P0重构交付状况.md
docs/AI分析P0重构接口冻结.md
docs/AI分析修复后复测与剩余优化方案.md
docs/AI分析功能实测审计与重构修复方案.md
# 2026-09-23 追加：**审计 / 指引 / 方案类**的工作文档，用户明确不提交（文件留在工作区）。
#
# 判据照旧是上面那条 ——「凡是被 tests/ 引用到的文件都不许进这个名单」。这四份逐条核过：
#   * `AI分析全链路复测与强制改造指引` 被两个测试文件**在 docstring 里引用**（引的是
#     规格出处，不是 `exec`/`read` 它），所以不入库**不会**造成本仓警告的那种
#     「本地绿、CI 红」；代价只是那两处引用的路径在一个新克隆里指不到东西。
#   * 其余三份 `tests/` 与 `scripts/` 下零引用。
# **产品文档不在此列**：`docs/AI分析使用说明.md` 必须留在库里 —— README 链着它，
# 而且 `tests/test_ai_docs_stay_in_sync.py` 会读它并断言链接在场。
docs/AI分析任务E优化与Run22-23实测复核报告.md
docs/AI分析全链路复测与强制改造指引-2026-09-23.md
docs/AI分析待修问题与修复方案.md
docs/DeepSeek-Harness缓存机制对照与AI分析优化指引.md
